#include "../lib/ia.h"
#include "../lib/play1.h"
#include "../lib/jogar.h"

// Chama função menu, que contém o loop para permanecer no jogo até que o usuário queira sair.
int main(){	
	srand(time(NULL));
	menu();
	return 0;
}
