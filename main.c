#include "ia.h"
#include "play1.h"
#include "jogar.h"
// chama fun��o menu a qual contem o loop para permanecer no jogo ate que o usuario queira sair
int main(){	
	srand(time(NULL));
	menu();
	return 0;
}
